section.signup-child
  .signup-header
    h3 PREMIUM
    .signup-pricing
      span.pricing $ #{plan.price}
      | /month
      p That’s only 13.30$ per meal
  .signup-features
    ul
      li
        i.fas.fa-check.small-icon  
        | 1 meal every day
      li
        i.fas.fa-check.small-icon  
        | Order 24/7
      li
        i.fas.fa-check.small-icon  
        | Access to newest creations
      li
        i.fas.fa-check.small-icon  
        | Free delivery
    .signup-button
        if user
            a.btn.btn-full.bookPlan(href=`javascript:void(0)` planId=`${plan["_id"]}`) I'm hungry
        else
            a.btn.btn-full(href=`/login`) I'm hungry